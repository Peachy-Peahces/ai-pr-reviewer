# Demo 演示脚本 — AI PR Reviewer

> 建议录制时长：3-5 分钟 | 提前准备好 PR URL 和 config.json

---

## 🎬 录制前准备

1. 启动 Streamlit：`streamlit run src/ui/streamlit_app.py`
2. 确保 `config.json` 已配置 DeepSeek API Key
3. 准备一个 GitHub PR URL（或直接用离线 Mock）
4. 屏幕录制备好，浏览器全屏，隐藏书签栏

---

## 📝 分镜脚本（约 4 分钟）

### 镜头 1：项目介绍（30 秒）

> 画面：README.md + 项目目录

"大家好，我做的项目是 **AI PR Reviewer**——基于大语言模型的 GitHub PR 自动代码审查工具。

Code Review 是团队开发中很容易被忽略的一环——要么 reviewer 太忙没空认真看，要么走马观花只点 Approve。这个项目的初衷就是：**让 AI 帮我们做第一轮代码审查**，自动发现安全漏洞、逻辑 Bug、性能隐患，然后把建议精准贴到对应的代码行上。"

---

### 镜头 2：核心流程演示（60 秒）

> 画面：浏览器打开 localhost:8501

"来看看效果。前端用 Streamlit 构建，界面很简洁。

输入 GitHub PR URL——我准备了一个真实的 PR。右上角可以**切换模型**，默认 DeepSeek V4 Flash 快速模式，也能切到 V4 Pro 高精度模式。

点击「开始审查」——它会自动：获取 PR 信息 → 拉取 diff → 解析文件改动 → 发给 DeepSeek 做多维度审查。PR 比较大的时候，会按文件边界自动拆分成多批处理，避免超 Token。"

（点击按钮，展示流程）

"审查完成了！"

---

### 镜头 3：报告展示——行级精准定位（50 秒）

> 画面：审查报告主页 + 问题 Tab

"看报告。上方是一个**大圆形评分徽章**——6 分、橙色，说明有些问题需要关注。颜色直接传达严重程度：绿≥7、橙≥5、红<5。

切换到「问题」标签页——列出了所有发现，按 🔴严重 / 🟡警告 / 🔵提示 / 💡建议 分类。

展开一个严重问题——"

（展开 SECURITY-001 问题）

"**这是行级精准定位**：不仅告诉你'有硬编码密码'，还精确到了具体行号，▶ 箭头指向问题代码，展示上下文。评委一眼就能看到 AI 精确找到了问题代码。

下面还有具体的修复建议——开发者可以直接照着改，这就是 actionable 的反馈。"

---

### 镜头 4：周边功能（40 秒）

> 画面：优点/JSON Tab → 导出 → 发布到 PR

"「优点」Tab 肯定做得好的地方——不是只找茬。「JSON」Tab 展示结构化数据，方便接入 CI/CD。

底下可以**导出 Markdown 或 HTML 报告**——HTML 可以直接打印成 PDF。

如果有 GitHub Token，还能**一键把审查结果发布到 PR 评论区**，开发者直接在 PR 页面就能看到，不需要登录这个工具。"

---

### 镜头 5：侧栏——主题切换 + 审查历史（25 秒）

> 画面：展示侧栏

"侧栏最上面是**深色/浅色主题切换**——点一下就能切，CSS 实时注入不用刷新。

下面是**审查历史**——每次审查自动保存。我跑过几次，点任意一条就能完整恢复那次审查。演示时非常实用：提前跑好几轮，演示中一键切换展示不同结果。"

（点击历史记录，展示恢复效果）

---

### 镜头 6：总结亮点（30 秒）

> 画面：回到代码或 README

"总结一下：

**第一，工程化程度高。** Diff 分块、错误处理、代理支持、配置管理——不是简单的 API demo。

**第二，行级精准定位。** AI 返回的问题精确到行，前端展示代码上下文——这是这个项目的核心技术亮点。

**第三，完整的产品体验。** 从输入 PR → 选模型 → 审查 → 定位 → 导出 → 发布，全流程打通。深色模式、历史记录这些细节也打磨到位。

**第四，规范的开发流程。** 每个功能独立分支、语义化 commit、完整的 PR 记录——评委可以看我的 GitHub 提交历史。

谢谢大家！"

---

## 💡 录制技巧

| 要点 | 说明 |
|------|------|
| **先跑几次审查** | 让历史记录有数据，侧栏更丰富 |
| **默认深色模式** | 录屏效果更专业 |
| **鼠标慢一点** | 让评委看清楚操作 |
| **关键画面滞留 2 秒** | 评分徽章、行号上下文、历史列表 |
| **结尾停留 3 秒** | 给评委消化印象 |

## 🔧 备用方案

如果网络不稳，点 **🧪 离线测试**——用内置示例代码生成完整报告，不依赖任何 API，百分百可靠。
